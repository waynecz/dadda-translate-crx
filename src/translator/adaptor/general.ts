import { TranslateResult as IGeneralTranslation } from "translation.js/declaration/api/types";
import { IStdTranslateResult } from "@models/dadda";

/**
 * Adaptor for translation.js (youdao / google)
 */
 export default function generalAdaptor(rawResult: IGeneralTranslation): IStdTranslateResult {
  return
 }